# Remove Application Credential to test fallback
apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - command: kubectl delete secret ac-nova-secret --ignore-not-found=true
  - command: kubectl delete keystoneapplicationcredential ac-nova --ignore-not-found=true
