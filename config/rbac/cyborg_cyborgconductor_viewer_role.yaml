# This rule is not used by the project nova-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants read-only access to cyborg.openstack.org resources.
# This role is intended for users who need visibility into these resources
# without permissions to modify them. It is ideal for monitoring purposes and limited-access viewing.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: nova-operator
    app.kubernetes.io/managed-by: kustomize
  name: cyborg-cyborgconductor-viewer-role
rules:
- apiGroups:
  - cyborg.openstack.org
  resources:
  - cyborgconductors
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - cyborg.openstack.org
  resources:
  - cyborgconductors/status
  verbs:
  - get
